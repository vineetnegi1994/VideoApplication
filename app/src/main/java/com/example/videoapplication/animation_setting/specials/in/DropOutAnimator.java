package com.example.videoapplication.animation_setting.specials.in;

import android.animation.ObjectAnimator;
import android.view.View;

import com.example.videoapplication.animation_setting.BaseViewAnimator;
/*
import com.example.videoapplication.animation_setting.other.Glider;
import com.example.videoapplication.animation_setting.other.Skill;

*/

/*
public class DropOutAnimator extends BaseViewAnimator {
    @Override
    protected void prepare(View target) {
        int distance = target.getTop() + target.getHeight();
        getAnimatorAgent().playTogether(
                ObjectAnimator.ofFloat(target, "alpha", 0, 1),
                Glider.glide(Skill.BounceEaseOut, getDuration(), ObjectAnimator.ofFloat(target, "translationY", -distance, 0))
        );
    }
}
*/
